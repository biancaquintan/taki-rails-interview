<%= form_for items, :url => items_path, :html => { :multipart => true } do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <br>
  <br>

  <%= f.label :price %>
  <%= f.text_field :price %>

  <br>
  <br>


  <%= submit_tag "Add", class: "btn btn-primary" %>
<% end %>